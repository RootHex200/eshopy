
import 'package:flutter/material.dart';

class Languagepage extends StatelessWidget {
  const Languagepage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('LanguagePage Page'), )
    );
  }
}