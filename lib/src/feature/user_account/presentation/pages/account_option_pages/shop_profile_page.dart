

import 'package:flutter/material.dart';

class Shopprofile extends StatelessWidget {
  const Shopprofile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Shopprofile"),
    );
  }
}