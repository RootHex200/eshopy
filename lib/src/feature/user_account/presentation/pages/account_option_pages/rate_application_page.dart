
import 'package:flutter/material.dart';

class Rateapplication extends StatelessWidget {
  const Rateapplication({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Rateapplication"),
    );
  }
}