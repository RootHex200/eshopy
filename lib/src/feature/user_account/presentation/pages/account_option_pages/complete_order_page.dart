



import 'package:flutter/material.dart';

class Completorder extends StatelessWidget {
  const Completorder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Completorder"),
    );
  }
}